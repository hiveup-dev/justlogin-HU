<li class='clearfix record <%= cycle("on", "on-hover") %>' id="<%= dom_id(employee) -%>">
  <span class='title'>
    <%= employee.name %>
  </span>

  <span class='preview'></span>

  <span class='actions'>
    <%= action_icon(:preview, refinery.policies_employee_path(employee), t('.view_live_html')) %>
    <%= action_icon(:edit,    refinery.edit_policies_admin_employee_path(employee), t('.edit') ) %>
    <%= action_icon(:delete,  refinery.policies_admin_employee_path(employee), t('.delete'),
      { class: "cancel confirm-delete",
        data: {confirm: t('message',  scope: 'refinery.admin.delete', title: employee.name)}}  ) %>
  </span>
</li>
