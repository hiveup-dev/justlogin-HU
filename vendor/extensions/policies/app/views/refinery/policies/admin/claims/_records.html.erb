<%= render 'refinery/admin/search_header', :url => refinery.policies_admin_claims_path %>
<div class='pagination_container'>
  <% if @claims.any? %>
    <%= render 'claims' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
