<%= render 'refinery/admin/search_header', :url => refinery.products_admin_user_submissions_path %>
<div class='pagination_container'>
  <% if @user_submissions.any? %>
    <%= render 'user_submissions' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
