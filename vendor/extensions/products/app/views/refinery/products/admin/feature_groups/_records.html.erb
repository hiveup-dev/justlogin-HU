<%= render 'refinery/admin/search_header', :url => refinery.products_admin_feature_groups_path %>
<div class='pagination_container'>
  <% if @feature_groups.any? %>
    <%= render 'feature_groups' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
