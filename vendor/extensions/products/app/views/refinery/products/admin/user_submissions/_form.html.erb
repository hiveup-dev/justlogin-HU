<%= simple_form_for [refinery, :products_admin, @user_submission] do |f| -%>
    <%= render '/refinery/admin/error_messages',
               :object => @user_submission,
               :include_object_name => true %>

    <%= f.input :first_name %>
    <%= f.input :last_name %>
    <%= f.input :email %>
    <%= f.input :company %>
    <%= f.input :phone_number %>
    <%= f.input :other_inquiries %>
    <p>
      <b>Plans:</b>
      <%#= @user_submission.benefits_names %>
    </p>

    <p>
      <b>Products:</b>
      <%= @user_submission.product_names %>
    </p>

    <%= render '/refinery/admin/form_actions', :f => f,
               :continue_editing => false,
               :delete_title => t('delete', :scope => 'refinery.user_submissions.admin.user_submissions.user_submission'),
               :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @user_submission.first_name) -%>
<% end -%>
